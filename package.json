{
  "name": "@skemati-ca/docs",
  "version": "0.1.0",
  "description": "Local MCP server for surgical editing of Office documents (DOCX, XLSX, PPTX)",
  "license": "GPL-3.0-or-later",
  "author": "Daniel Gómez-Padilla <daniel@skemati.ca>",
  "repository": {
    "type": "git",
    "url": "https://github.com/skemati-ca/skematica-docs"
  },
  "type": "module",
  "bin": {
    "skematica-docs": "./dist/index.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "check": "npm run lint && npm run typecheck && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
